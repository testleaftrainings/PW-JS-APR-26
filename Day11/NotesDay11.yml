📑 Agenda for Week 6 Day 1: Page Object Model in TypeScript

🎯 Focus: Mastering POM in TypeScript

📆 30th Nov, Saturday

🕝 Session Highlights

⿡ Recap
Quick review of previous learnings.

1. Access Modifiers
Understanding public, private, and protected access modifiers.
Examples: Controlling access to class properties and methods.

Breakout/ Practice 

2. Polymorphism

Understanding how the same method can behave differently in different classes.
Examples:
Method Overriding using inheritance.
Runtime Polymorphism in TypeScript.

3. Inheritance
Exploring the concept of inheritance for code reuse.
Examples: Implementing single and multilevel inheritance.

Breakout/ Practice 

4. POM (Page Object Model)
Design pattern to enhance test maintainability and reusability.
Examples: Creating page classes and using them in test cases.

-------------------------------------------------------------------------------


Notes :
------
export issue can resolved following below steps :

In package.json change type : "CommonJS" => ES Modules

And in the dericed class/ child class while import use file extension .js  like below

import { EmployeeSignUp } from "../Day11/01-accessModifiers.js";
 
Understanding CommonJS vs ES Modules in Node.js

CommonJS vs ES Modules Comparison


| Feature                  | CommonJS       | ES Modules |
| ------------------------ | -------------- | ---------- |
| Import                   | require()      | import     |
| Export                   | module.exports | export     |
| File Extension           | Optional       | Mandatory  |
| Default in Old Node.js   | Yes            | No         |
| Browser Support          | No             | Yes        |
| Modern Standard          | No             | Yes        |
| Recommended for New Apps | No             | Yes        |


---

AccessModifiers:

1. public : can be accessed inside and outside of the class
2. protected: can be accessed inside the class as well as the related class --> relationship--> inherit
3. private: not accessible outisde the class --> give permission to access or to rewrite "get" and "set"
4. readonly: constant/final --> cannot change the assigned value
5. static: belongs to the class and not to the object --> can be accessed without creating the object of the class.

🔹 When do we use static in a TypeScript framework?

We use static when the data or behavior is:

✅ Common for the entire framework
✅ Does NOT depend on object state
✅ Utility / helper / configuration
✅ Shared across tests, pages, or services

1️⃣ Static for Constants & Configuration
📌 Use case

URLs
Timeouts
Environment names
Browser types

class Config {
  static BASE_URL = "https://leaftaps.com";
  static TIMEOUT = 5000;
}

Usage (no object needed)
await page.goto(Config.BASE_URL);


✔ Single source of truth
✔ Easy to maintain

*****************************************************************************************
About super keyword :

### `super` keyword in TypeScript — Simple & Clear Definition

In **TypeScript (and JavaScript)**, the **`super` keyword** is used in a **child (derived) class** to **access the parent (base) class**.

It is mainly used for **two purposes**:

---

## 1️⃣ Calling the Parent Class Constructor

When a class **extends** another class, the child class **must call `super()`** before using `this`.

### ✅ Definition

> **`super()` calls the constructor of the parent class.**

### Example

```ts
class LoginPage {
  username: string;

  constructor(username: string) {
    this.username = username;
  }
}

class DashboardPage extends LoginPage {
  role: string;

  constructor(username: string, role: string) {
    super(username); // calls parent constructor
    this.role = role;
  }
}

const dp = new DashboardPage("admin", "manager");
```

📌 **Important rule**
👉 `super()` **must be the first statement** in the child constructor.

---

## 2️⃣ Accessing Parent Class Methods or Properties

### ✅ Definition

> **`super.methodName()` is used to call a method from the parent class.**

### Example

```ts
class BasePage {
  navigate() {
    console.log("Navigating to page");
  }
}

class HomePage extends BasePage {
  openHome() {
    super.navigate(); // calling parent method
    console.log("Home page opened");
  }
}

const home = new HomePage();
home.openHome();
```

---

## When Do We Use `super`? (Real-Time Use Case)

Since you work with **Playwright + Page Object Model**, this is very common 👇

```ts
class BasePage {
  protected page;

  constructor(page) {
    this.page = page;
  }

  async click(locator: string) {
    await this.page.click(locator);
  }
}

class LoginPage extends BasePage {
  constructor(page) {
    super(page); // pass page to BasePage
  }

  async login() {
    await this.click("#login");
  }
}
```

🔹 `super(page)` passes the Playwright `page` object to the base class
🔹 Avoids duplicate code
🔹 Improves framework design

---

## Definition

> **The `super` keyword in TypeScript is used to call the parent class constructor and access parent class methods or properties from a child class.**

*****************************************************************************************

What is Method Overriding?

Method overriding means:

A child class provides its own implementation of a method that already exists in the parent class.

Conditions:

Must use class inheritance (extends)

Same method name

Same parameters

Real Project-Level Scenario in Playwright

Imagine you have:

BasePage.ts
LoginPage.ts
SalesforceLoginPage.ts

You want common functionality in BasePage, but allow specific pages to customize behavior.

Example 1 — BasePage (Parent Class)

BasePage.ts

export class BasePage {
    async navigate(url: string) {
        console.log("Navigating to:", url);
    }

    async verifyPageTitle(title: string) {
        console.log("Verifying title:", title);
    }
}


Example 2 — LoginPage (Child Class)

import { BasePage } from "./BasePage";

export class LoginPage extends BasePage {

    async navigate(url: string) {
        console.log("Custom Login Navigation to:", url);
    }
}


Here:

navigate() method exists in BasePage
LoginPage overrides it.

What Happens?

If you do:

const login = new LoginPage();
login.navigate("https://example.com
");

Output:
Custom Login Navigation to: https://example.com

Child method overrides parent method.

********************************************************************************************************

Inheritance :

1. Single Inheritance :

classA{} 
classB extends A{}

Parent to Child

2. Multilevel Inheritance :

class A{}

class B extends A{}
class C extends B{}

Grandparent A -> parent B -> child C

3. Hierarchical inheritance 

class A{}

class B extends A{}
class C extends A{}

Parent having 2 children :

Parent Class A --> Class B
               --> Class C

4. Hybrid Inheritance

A combination of two or more types of inheritnace (e.x : multilevel and Hierarchical)


5. Multiple Inheritance
Typescript does not support multiple inheritnace using classes.
Can be achieved through interface.

*************************************************************************************************

Page Object Model :

POM means:

Each web page (or module) of your application is represented by a separate class,
and that class contains locators and methods that act on that page.

While desiging POM things to do:

1. Inheritance => mutilevel inheritance
2. Abstraction => playwright wrapper class
3. Data parameterization => json, env, faker(synthetic data)
4. Custom Fixture => Used to create ready made objects

Inheritance

abstract class -> loginPage --> welcomePage --> leadPage --> CreateLeadPage --> ViewLeadPage


faker library installation :

npm install @faker-js/faker@8.4.1


Naming Conventions in JavaScript

=> File Names :
--------------

camelCase

Example:

loginPage.js
userProfile.js

=> Variables :
-------------

camelCase

Start with a lowercase letter

Example:

isActive
userName
totalAmount

=> Classes
-----------

PascalCase

Start with an uppercase letter

Example:

class LoginPage { }
class UserService { }
