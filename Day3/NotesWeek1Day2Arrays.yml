

Notes:
------

Arrays in JavaScript :
----------------------


Arrays :
--------
1. Dynamic size
2. Heterogenous elements.

--------------------------

Difference between slice() and splice()

slice(start, endIndex)
arr.slice(1, 4)


Means:

Start at index 1, stop before index 4

splice(start, deleteCount)
arr.splice(1, 3)


Means:

Start at index 1, remove 3 elements

So:

["a","b","c","d","e"]
     ↑ start=1

remove 3 → b, c, d


Remaining array:

["a","e"]

Side-by-side
Method	Second argument means
slice(1,4)	Stop at index 4 (exclusive)
splice(1,3)	Remove 3 elements

 Interview line

“slice() uses start and end index, but splice() uses start index and delete count.”

--------------------------------------------------------------------------------------------------------------------

Sort Method internal mechanism:
-------------------------------

let numberArray = [10, 8, 9, 7];
numberArray.sort((a, b) => a - b);


### 🟢 Step 1: Initial Array

[10, 8, 9, 7]
---

### 🟢 Step 2: First Comparisons

1. Compare **10 and 8**

   * a = 10, b = 8 → `10 - 8 = 2` (positive)
   * Swap → `[8, 10, 9, 7]`//new array -1

2. Compare **10 and 9**

   * a = 10, b = 9 → `10 - 9 = 1` (positive)
   * Swap → `[8, 9, 10, 7]` //new array -2

3. Compare **10 and 7**

   * a = 10, b = 7 → `10 - 7 = 3` (positive)
   * Swap → `[8, 9, 7, 10]`

---

### 🟢 Step 3: Next Pass

1. Compare **8 and 9**

   *  a = 8,b = 9 → `8 - 9 = -1` (negative)
   * Keep order → `[8, 9, 7, 10]`

2. Compare **9 and 7**

   * a = 9, b = 7 → `9 - 7 = 2` (positive)
   * Swap → `[8, 7, 9, 10]`

3. Compare **9 and 10**

   * a = 9, b = 10 → `9 - 10 = -1` (negative)
   * Keep order → `[8, 7, 9, 10]`

---

### 🟢 Step 4: Another Pass

1. Compare **8 and 7**

   * a = 8, b = 7 → `8 - 7 = 1` (positive)
   * Swap → `[7, 8, 9, 10]`

Now everything is in order 

---

### Final Sorted Array ascending order array

[7, 8, 9, 10]

---

Quick Recap:

* `a` and `b` are just **two numbers being compared**.
* The compare function `(a - b)` tells JavaScript whether to swap them.


🔹 What is Set in JavaScript?

A Set is a built-in object that stores only unique values (no duplicates allowed).

✅ Simple Example (Best for learners)
👉 Remove duplicates from an array
let numbers = [1, 2, 2, 3, 4, 4, 5];

// Convert array → Set → back to array
let uniqueNumbers = [...new Set(numbers)];

console.log(uniqueNumbers);
🟢 Output:
[1, 2, 3, 4, 5]